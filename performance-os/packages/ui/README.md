# @performance-os/ui

Reusable React Native UI primitives. The MVP's components live in
`apps/mobile/src/components/ui.tsx` (Card, Stat, Button, ScoreRing, MacroBar, Skeleton,
EmptyState). Promote them here when a second surface needs them.
