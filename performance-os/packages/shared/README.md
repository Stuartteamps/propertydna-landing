# @performance-os/shared

Cross-surface constants and types (branding, API contracts). Kept framework-agnostic so the
mobile app — and any future web dashboard — share one definition. The mobile app currently
mirrors API types in `apps/mobile/src/api/types.ts`; consolidate here as more surfaces are added.
