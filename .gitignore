# Dependencies
node_modules/

# Environment files (local secrets/config)
.env
.env.local
.env.*.local

# Build output
dist/
build/
.output/

# Lock files (project uses pnpm-lock.yaml)
package-lock.json

# macOS
.DS_Store

# Editor
.vscode/
.idea/

# Logs
*.log
npm-debug.log*

# Local Netlify folder
.netlify
AuthKey_*.p8
*.p8

# OAuth credentials and tokens — never commit
tools/gmail-cleanup/credentials_*.json
tools/gmail-cleanup/token_*.json
tools/**/*.json
!tools/browser-agent/data/content-calendar.json
!tools/browser-agent/data/post-queue.json
*.credentials.json
*_token.json
.claude/settings.local.json

# Android signing — NEVER commit keystores
*.jks
*.keystore
android/app/release/
**/local.properties

# npm tokens — never commit
.npmrc
**/.npmrc
